pull_request_rules:
  - name: BB clean author bot permission boundary
    conditions:
      - title = BB clean author bot gate canary
    actions:
      merge:
        method: merge
        merge_bot_account: "{{ author }}"
